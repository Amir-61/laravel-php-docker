version: '3.8'

services:
  server:
    image: nginx:stable-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 8000:80
  # php:
  # mysql:
  # comproser:
  # artisan:
  # npm: